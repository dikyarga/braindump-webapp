import React from "react";

import { Container, Main } from "@components";

const Home: React.FC = () => {
    return (
        <Container>
            <Main />
        </Container>
    );
};

export default Home;
